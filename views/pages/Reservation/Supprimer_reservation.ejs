<div class="container">
  <div id="myModalDelete" role="dialog" class="modal fade">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <button type="button" data-dismiss="modal" class="close">&times;</button>
          <h4 class="modal-title">Modification reservation</h4>
        </div>
        <div class="modal-body">
          <!--form method="post" action="/reservation/modifier/">
                <input class="form-control" type="hidden" value="<%= req.session.user %>" id="Matriculeinsert" name="matricule" required>
            <div class="form-group row">
              <label for="Objetreservation" class="col-xs-4 col-form-label">Objet</label>
              <div class="col-xs-8">
                <input class="form-control" type="text" value="" id="Objetreservation" name="object" required>
              </div>
            </div>
            <div class="form-group row">
              <label for="Departement" class="col-xs-4 col-form-label">Departement</label>
              <div class="col-xs-8">
                <select name="dep" class="form-control" data-id="<%= Departement.id_reservation %>" data-model="departement" id="Departement" required>
                  <% Departement.forEach (function(dep) { %>
                  <option value="<%= dep.id_departement %>"><%= dep.libelle %></option>
                  <% }) %>
                </select>
              </div>
            </div>
            <div class="form-group row">
              <label for="salle" class="col-xs-4 col-form-label">Salle</label>
              <div class="col-xs-8">
                <select name="salle" class="form-control" id="salle" required >
                  <option value="Grande Salle">Grande Salle</option>
                  <option value="Salle de Formation">Salle de Formation</option>
                  <option value="Salle de Formation RDJ">Salle de Formation RDJ</option>
                </select>
              </div>
            </div>
            <div class="form-group row">
              <label class="col-xs-4 col-form-label">Date</label>
              <div class="col-xs-8">
                <input class="form-control" type="text" name="date_reserv" id="" placeholder = "Date Debut" value="" required>

              </div>
            </div>
            <div class="form-group row">
              <label class="col-xs-3 col-form-label" >De</label>
              <div class="col-xs-4">
                heure<select name="heure_deb" class="form-control" id="" required>
                  <%
                  var i=0;
                  for(i=1;i<25;i++){%>
                  <option value="<%= i%>"><%= i %></option>
                  <%}%>
                </select>
              </div>
              <div class="col-xs-4">
                minute<select name="min_deb" class="form-control" id="" required>
                  <%
                  var i=0;
                  for(i=0;i<60;i++){%>
                  <option value="<%= i%>"><%= i %></option>
                  <%}%>
                </select>
              </div>
            </div>
            <div class="form-group row">
              <label for="example-heurefin-input-input" class="col-xs-3 col-form-label">à</label>
              <div class="col-xs-4">
                heure<select name="heure_fin" class="form-control" id="example-heuredep-input" required>
                  <%
                  var i=0;
                  for(i=1;i<25;i++){%>
                  <option value="<%= i%>"><%= i %></option>
                  <%}%>
                </select>
              </div>
              <div class="col-xs-4">
                minute<select name="min_fin" class="form-control" id="example-heuredep-input" required>
                  <%
                  var i=0;
                  for(i=0;i<60;i++){%>
                  <option value="<%= i%>"><%= i %></option>
                  <%}%>
                </select>
              </div>
              <input type="hidden" value="<%= req.session.nom %>" name="nom_personnel">
              <input type="hidden" id="id_reservation" class="" name="id_reservation" >
            </div>
            <center><input type="submit" class="btn btn-primary" value="Modifier"></center>
          </form-->
        </div>
        <div class="modal-footer">
          <form action="/reservation/suppression/" method="post">
            <input type="hidden" id="id_tost" class="" name="ud_reservation" >
            <input type="submit" class="btn btn-warning" value="Annuler Reservation Salle">
          </form>
          <button type="button" data-dismiss="modal" class="btn btn-default">Close</button>
        </div>
      </div>
    </div>
  </div>
</div>
</div>

<script src="/js/dependencies/sails.io.js"></script>
<!--script src="/js/jquery-1.12.4.js"></script-->
<!--script src="/js/jquery-ui.min.js"></script-->
<script src="/bootstrap/js/bootstrap.min.js"></script>

<!--script>

  function loadData(){
    var dateDeb = $( "#datetimd" ).val();
    var dateFin = $( "#datetimf" ).val();
    $.ajax({
      type: "GET",
      url: "/ajaxAlm?datedeb="+dateDeb+"&datefin="+dateFin,
      beforeSend: function(xhr){
        $( "#load").show();
      },
      success: function(msg){
        $("#table-response").html(msg);
        $( "#load").hide();
      },
      error: function (error) {
        alert('error; ' +error);
      }
    });
  }

  $(function() {
    $( "#datetimd" ).datepicker({
      dateFormat: 'yy-mm-dd'
    });
    $( "#datetimf" ).datepicker({
      dateFormat: 'yy-mm-dd'
    });
  } );

</script-->
